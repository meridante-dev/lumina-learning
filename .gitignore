# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local harness config (machine-specific paths)
.claude/

# Node (for future Next.js port)
node_modules/
.next/
dist/
*.log
.env
.env.local
CLAUDE.md
FOR-THE-LOOP.md

# Working media that must not enter git history on a public repo.
# 22MB of promo renders + three superseded site backups were swept in by an
# `add -A`; they stay on disk, they just don't ship. Publish a promo cut
# deliberately (and prefer a CDN link) rather than committing the master.
collateral/promo/
collateral/site-backups/
collateral/site-v2/
.stale/
exports/
